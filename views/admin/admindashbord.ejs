<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />
    <link rel="stylesheet" href="/styles/styles.css" />
    <title>Bootstap 5 Responsive Admin Dashboard</title>
</head>

<body>
    <div class="d-flex" id="wrapper">
        <!-- Sidebar -->
        <div class="bg-white" id="sidebar-wrapper">
            <div class="sidebar-heading text-center py-4 primary-text fs-4 fw-bold text-uppercase border-bottom"><i
                    class="fas fa-user-secret me-2"></i>Codersbite</div>
            <div class="list-group list-group-flush my-3">
                <a href="/admin" class="list-group-item list-group-item-action bg-transparent second-text active"><i
                        class="fas fa-tachometer-alt me-2"></i>Dashboard</a>
                <a href="/admin/addproduct" class="list-group-item list-group-item-action bg-transparent second-text fw-bold"><i
                        class="fas fa-project-diagram me-2"></i>Add Product</a>
                <a href="/admin/catogerylist" class="list-group-item list-group-item-action bg-transparent second-text fw-bold"><i
                        class="fas fa-project-diagram me-2"></i>Catogery</a>
                <a href="#" class="list-group-item list-group-item-action bg-transparent second-text fw-bold"><i
                        class="fas fa-chart-line me-2"></i>Analytics</a>
                <a href="#" class="list-group-item list-group-item-action bg-transparent second-text fw-bold"><i
                        class="fas fa-paperclip me-2"></i>Salse Reports</a>
                <a href="/admin/userlist" class="list-group-item list-group-item-action bg-transparent second-text fw-bold"><i
                        class="fas fa-shopping-cart me-2"></i>Users</a>
                <a href="/admin/Productlist" class="list-group-item list-group-item-action bg-transparent second-text fw-bold"><i
                        class="fas fa-gift me-2"></i>Products</a>
                <a href="/admin/coupens" class="list-group-item list-group-item-action bg-transparent second-text fw-bold"><i
                        class="fas fa-comment-dots me-2"></i>Coupens</a>
                <a href="#" class="list-group-item list-group-item-action bg-transparent second-text fw-bold"><i
                        class="fas fa-map-marker-alt me-2"></i>Offers</a>
                <a href="/admin/banner" class="list-group-item list-group-item-action bg-transparent second-text fw-bold"><i
                        class="fas fa-map-marker-alt me-2"></i>Banner</a>
                <a href="#" class="list-group-item list-group-item-action bg-transparent text-danger fw-bold"><i
                        class="fas fa-power-off me-2"></i>Logout</a>
            </div>
        </div>
        <!-- /#sidebar-wrapper -->

        <!-- Page Content -->
        <div id="page-content-wrapper">
            <nav class="navbar navbar-expand-lg navbar-light bg-transparent py-4 px-4">
                <div class="d-flex align-items-center">
                    <i class="fas fa-align-left primary-text fs-4 me-3" id="menu-toggle"></i>
                    <h2 class="fs-2 m-0">Dashboard</h2>
                </div>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle second-text fw-bold" href="#" id="navbarDropdown"
                                role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-user me-2"></i>John Doe
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <li><a class="dropdown-item" href="#">Profile</a></li>
                                <li><a class="dropdown-item" href="#">Settings</a></li>
                                <li><a class="dropdown-item" href="#">Logout</a></li>
                            </ul>
                        </li>
                    </ul>
                </div>
            </nav>

            <div class="container-fluid px-4">
                <div class="row g-3 my-2">
                    <div class="col-md-3">
                        <div class="p-3 bg-white shadow-sm d-flex justify-content-around align-items-center rounded">
                            <div>
                                <h3 class="fs-2">9</h3>
                                <p class="fs-5">Products</p>
                            </div>
                            <i class="fas fa-gift fs-1 primary-text border rounded-full secondary-bg p-3"></i>
                        </div>
                    </div>

                    <div class="col-md-3">
                        <div class="p-3 bg-white shadow-sm d-flex justify-content-around align-items-center rounded">
                            <div>
                                <h3 class="fs-2">4920</h3>
                                <p class="fs-5">Sales</p>
                            </div>
                            <i
                                class="fas fa-hand-holding-usd fs-1 primary-text border rounded-full secondary-bg p-3"></i>
                        </div>
                    </div>

                    <div class="col-md-3">
                        <div class="p-3 bg-white shadow-sm d-flex justify-content-around align-items-center rounded">
                            <div>
                                <h3 class="fs-2">3899</h3>
                                <p class="fs-5">Delivery</p>
                            </div>
                            <i class="fas fa-truck fs-1 primary-text border rounded-full secondary-bg p-3"></i>
                        </div>
                    </div>

                    <div class="col-md-3">
                        <div class="p-3 bg-white shadow-sm d-flex justify-content-around align-items-center rounded">
                            <div>
                                <h3 class="fs-2">%25</h3>
                                <p class="fs-5">Increase</p>
                            </div>
                            <i class="fas fa-chart-line fs-1 primary-text border rounded-full secondary-bg p-3"></i>
                        </div>
                    </div>
                </div>

                
    <div class="container-fluid d-flex gap-4" >


          
        

        
        
        <div style="overflow-y: auto;" class="w-100 d-flex align-items-center align-items-center ">
          <div class="row mt-3 ml-lg-4">
            <div class="col-lg-6 col-12 mb-3">
              <div class="row">
                <div class="col-12 mb-3">
                  <div class="container-fluid bg-white pt-3 pb-5">
                    <h5 class="text-success px-3 pt-3 pb-0"> Daily Orders</h5>
                    <p class="pl-3 pt-0 pb-3"><small>Our orders for the past month</small> </p>
                    <div class="d-flex justify-content-end">
                      <form>
                        <select class="selectpicker" id="filter1" onchange="filter()">
                          <option value="30" selected>Last 30 days</option>
                          <option value="14">Last 14 days</option>
                          <option value="7">Last 7 days</option>
                        </select>
                      </form>
                    </div>
                    <canvas id="daily"></canvas>
                  </div>
                </div>
                <div class="col-12">
                  <div class="container bg-white pt-3 pb-3 mt-1">
                    <h5 class="text-success px-3 pt-3 pb-0">Category Distribution</h5>
                    <p class="pl-3 pt-0 pb-0"><small>Our orders per category</small> </p>
                    <div class="col-10">
                      <canvas id="categories"></canvas>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            <div class="col-lg-6">
              <div class="row mb-3">
                <div class="col-5 p-0 mr-3 mb-3">
                  <div class="card border-0 p-3">
                    <h4 class="text-success">Customers</h4>
                    <h4 class="text-success"><i class="fa fa-user text-success"></i> <%= userCount %></h4>
                  </div>
                </div>
                <div class="col-5 px-lg-4 mb-3" style="box-sizing: content-box !important;">
                  <div class="card border-0 p-3">
                    <h4 class="text-success">Orders</h4>
                    <h4 class="text-success"><i class="fa fa-user text-success"></i> <%= orderCount %></h4>
                  </div>
                </div>
                <div class="col-lg-11 bg-white mb-2">
                  <div class="container bg-white py-3 pb-4">
                    <h5 class="text-success px-3 pt-3 pb-0"> Users joined</h5>
                    <p class="pl-3 pt-0 pb-3"><small>Our users this month vs last month</small> </p>
                    <canvas id="user"></canvas>
                  </div>
          
                </div>
                <div class="col-lg-11 p-2">
                  <div class="container bg-white pt-2 pb-0">
                    <h5 class="text-success px-3 pt-1 pb-0">Recent Transactions</h5>
                    <p class="pl-3 pt-0 pb-3"><small>Last few orders</small> </p>
                    <table class="table bg-white">
                      <thead>
                        <tr>
                          <th>Order No.</th>
                          <th>Order Date</th>
                          <th>Customer</th>
                          <th>Status</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% for (let i=0; i < 4; i++) { %>
                          <tr>
                            <td><%= i+1 %></td>
                            <td><%= orders[i].dateorder.toLocaleDateString() %></td>
                            <td><%= orders[i].user%></td>
                            <td><%= orders[i].orderstatus %></td>
                          </tr>
          
                          <% } %>
                      </tbody>
                    </table>
                    <a class="btn btn-sm btn-dark" href="/admin">Go to orders &rarr;</a>
                  </div>
                  <div class="container bg-white p-2 mt-2 rounded d-flex justify-content-between align-items-center">
                    <h6 class=" px-2"> Go to Sales report </h6>
                    <a href="/admin/salse-report" class="btn btn-success rounded text-white">Sales Report</a>
                  </div>
                </div>
              </div>
          
            </div>
          </div>
        </div>
        
        
        
        <script src="https://cdn.jsdelivr.net/npm/chart.js@3.5.1/dist/chart.min.js"></script>
        
        <script>
          if(new URLSearchParams(window.location.search).get('filter1')){
            document.getElementById('filter1').value = new URLSearchParams(window.location.search).get('filter1');
          }
        
        async function filter(){
          const filter1=document.getElementById('filter1').value;
          window.location.href=`/admin/dash-bord?filter1=${filter1}`
        }
        
            (async function () {
              const filter1=document.getElementById('filter1').value;
              const dailyOrderCount = JSON.parse('<%- JSON.stringify(daily) %>');
              const user = JSON.parse('<%- JSON.stringify(user) %>');
              const category = JSON.parse('<%- JSON.stringify(category) %>');
          
            const startDate = new Date();
          startDate.setDate(startDate.getDate() - filter1); // Subtract the filter1Value from the current date
          const endDate = new Date();
          
              const chartLabels = [];
              let currentDate = new Date(startDate);
              while (currentDate <= endDate) {
                const dateString = currentDate.toISOString().split('T')[0];
                chartLabels.push(dateString);
                currentDate.setDate(currentDate.getDate() + 1);
              }
          
              const dailyCounts = {};
              dailyOrderCount.forEach((daily) => {
                dailyCounts[daily._id] = daily.count;
              });
          
              const chartData = chartLabels.map((date) => dailyCounts[date] || 0);
          
              new Chart(
                document.getElementById('daily'),
                {
                  type: 'line',
                  data: {
                    labels: chartLabels,
                    datasets: [
                      {
                        label: 'Daily Order Count',
                        data: chartData,
                        backgroundColor: 'rgba(170, 128, 252, 0.2)', // Customize the area color
                        borderColor: 'rgba(170, 128, 252, 1)',
                        borderWidth: 1,
                        fill: 'origin', // Fill the area below the line
                      }
                    ]
                  },
                  options: {
                    responsive: true,
                    plugins: {
                  legend: {
                    position: 'bottom',
                  },
                },
                    scales: {
                      y: {
                        beginAtZero: true,
                        grid: {
                          display: false,
                        },
                      },
                      x: {
                        beginAtZero: true,
                        grid: {
                          display: false,
                        },
                        ticks: {
                        font: {
                          size: 10, // Set the font size for the chart labels
                        }
                      }
                      },
                      
                    },
                  }
                }
              );
              const categoryLabels = category.map(item => item.catogeryname);
              const categoryCounts = category.map(item => item.count);
              new Chart(
            document.getElementById('categories'),
            {
              type: 'pie',
              data: {
                labels: categoryLabels,
                datasets: [
                  {
                    label: 'Categories',
                    data: categoryCounts,
                    backgroundColor: [
                      'rgba(255, 99, 132, 0.8)',
                      'rgba(54, 162, 235, 0.8)',
                      'rgba(255, 206, 86, 0.8)',
                      'rgba(75, 192, 192, 0.8)',
                      'rgba(153, 102, 255, 0.8)',
                    ], 
                    borderColor: 'rgba(255, 255, 255, 1)', 
                    borderWidth: 1,
                  }
                ]
              },
              options: {
                responsive: true,
                plugins: {
                  legend: {
                    position: 'right',
                  },
                },
              }
            }
          );
        
          const userLabels = user.map(item => item._id);
          const userCounts = user.map(item => item.count);
          new Chart(
            document.getElementById('user'),
            {
              type: 'bar',
              data: {
                labels: userLabels,
                datasets: [
                  {
                    label: 'User Count',
                    data: userCounts,
                    backgroundColor: 'rgba(54, 162, 235, 0.8)',
                    borderColor: 'rgba(54, 162, 235, 1)',
                    borderWidth: 1,
                  }
                ]
              },
              options: {
                responsive: true,
                scales: {
                  y: {
                    beginAtZero: true,
                    grid: {
                      display: false, // Remove the y-axis grid lines
                    },
                  },
                  x: {
                    grid: {
                      display: false, // Remove the x-axis grid lines
                    },
                  },
                },
              }
            }
          );
        
            })();
          </script>
          
          
        
        
  
  


            

       
    </div>



            

       
    </div>
                
                

            </div>
        </div>
    </div>
    <!-- /#page-content-wrapper -->
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        var el = document.getElementById("wrapper");
        var toggleButton = document.getElementById("menu-toggle");

        toggleButton.onclick = function () {
            el.classList.toggle("toggled");
        };
    </script>
     <!-- axios cdn -->
     <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/1.2.3/axios.min.js"></script>

     <!-- sweet alert cdn -->
     <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
 
     <script>
         function Cancelorder(orderid) {
           Swal.fire({
             icon: "warning",
             title: "Are you sure?",
             text: "Cancel Order?",
             showCancelButton: true,
             confirmButtonColor: "#3085d6",
             cancelButtonColor: "#d33",
             confirmButtonText: "Ok",
           }).then((result) => {
             if (result.isConfirmed) {
               axios.get(`/admin/deleteorder?id=${orderid}`).then((result) => {
                 Swal.fire("Canceld.!", "Product has been Canceld.", "success").then((response) => {
                   location.reload();
                 })
               })
               
             }
           });
         }




         function Changestatus(selectElement) {
            const orderid = selectElement.dataset.orderid;
  const selectedStatus = selectElement.value;
           Swal.fire({
             icon: "warning",
             title: "Are you sure?",
             text: "Change status?",
             showCancelButton: true,
             confirmButtonColor: "#3085d6",
             cancelButtonColor: "#d33",
             confirmButtonText: "Ok",
           }).then((result) => {
             if (result.isConfirmed) {
               axios.get(`/admin/changesatatus?id=${orderid}&status=${selectedStatus}`).then((result) => {
                 Swal.fire("Changed.!", "Order status Changed.", "success").then((response) => {
                   location.reload();
                 })
               })
               
             }
           });
         }
       </script>
</body>

</html>